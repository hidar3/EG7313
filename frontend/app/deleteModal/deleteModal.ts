import { Component } from '@angular/core';

@Component({
  moduleId: module.id,
  selector: 'deleteModal-cmp',
  templateUrl: 'deleteModal.html'
})
export class deleteModalComponent { 
  name = 'deleteModalComponent'; 
}